# Tests

This directory is reserved for Archeon tests.

Day 0 keeps it intentionally light. Future work should add CLI tests, ingestion fixture tests, and citation behavior tests as Cognee integration arrives.
